buildscript {
    apply from: "dependencies_versions.gradle"
    apply from: "dependencies_classpath.gradle"
    apply from: "dependencies.gradle"
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}